// Variables globales ( ataque)
let ataque = parseInt(Math.random() * 100);
// Objeto monster
let monster = {
  name: mnombre,
  health: 100,
};

// objeto player
let player = {
  name: nombre,
  health: 100,
  potion: 2,
};

// objeto estadisticas
